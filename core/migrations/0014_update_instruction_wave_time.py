"""Update the activity instruction: mandatory wave start time, time-of-day rates."""
from django.db import migrations


INSTRUCTION_CONTENT = """Запись активности на форте

Сообщение должно начинаться с + и содержать 4 или 5 частей, разделённых между собой:

+<время> <разделитель> <тип> <разделитель> <ники> <разделитель> <время начала волны> <разделитель> <описание>

Порядок частей строгий: сначала время, затем тип, затем ники, затем время начала волны, затем описание. Если перепутать порядок (например, тип или ник поставить на место времени) — сообщение будет отклонено с ошибкой.

1. Время — сколько часов: 1, 2, 0,5 (через запятую) или 0.5 (через точку).
2. Тип — деф (оплачивается) или фарм (учитывается, но не оплачивается). Регистр не важен: ДЕФ, DEF, фарм — всё подойдёт.
3. Ники — один или несколько, через запятую. Только буквы и цифры (как в L2). Один тип — одно сообщение.
4. Время начала волны — ОБЯЗАТЕЛЬНО. Формат HH.MM или HH:MM (точка или двоеточие): например, 11.56 или 11:56. Если время не указать или указать неверно, бот ответит подсказкой, а запись не создастся.
5. Описание — любой текст (волна, замок, комментарий). Необязательно: можно не указывать.

Разделители (любой из перечисленных, можно с пробелами вокруг или без): | (вертикальная черта), - (дефис, удобнее с русской раскладки), – или — (тире). Просто пробелы между частями НЕ являются разделителем.

Примеры (все правильные):
+1 | деф | Swettka | 11.56 | Первая волна
+1 - деф - Swettka - 11:56 - Первая волна
+0,5 | фарм | Ostin, Pocomaxa | 23.10
+2 | ДЕФ | Swettka | 11.56

Мульти-ник (несколько игроков в одном сообщении):
Если в волне участвовало несколько человек, перечислите их ники через запятую в одном сообщении. Для каждого ника создастся отдельная запись активности с тем же временем и типом:
+1 | деф | Swettka, Vas, Dimas, Pocomaxa | 11.56 | Вторая волна
Такую «сводку волны» может прислать любой участник — она не привязывается к отправителю. Один и тот же ник внутри сообщения достаточно написать один раз (повторы игнорируются).

Одно сообщение = один тип (деф ИЛИ фарм). Если в волне были и деф, и фарм — пришлите два сообщения.

Выплата зависит от времени суток и настраивается на странице Settings (не в Django admin). По умолчанию: 00:01–08:00 = 100 кк/ч, 08:01–16:00 = 75 кк/ч, 16:01–00:00 = 50 кк/ч (кк — миллионы адены). Фарм не оплачивается (0). Если волна пересекает границу тарифного периода, оплата считается пропорционально. Сумма выплаты фиксируется при создании записи: изменение ставок влияет только на новые сообщения.

Если в сообщении ошибка (неизвестный формат, перепутан порядок частей, неверное время начала волны, конфликт ника) — бот ответит прямо в группе под вашим сообщением. Исправьте сообщение редактированием — бот пересчитает его заново (повторно не засчитает).
"""

# Захваченный в forward текст; в обратную миграцию возвращается через reverse.
previous_content = {}


def update_instruction_content(apps, schema_editor):
    Instruction = apps.get_model("core", "Instruction")
    global previous_content
    previous_content = dict(
        Instruction.objects.filter(slug="how-to-write-activity").values_list("pk", "content")
    )
    Instruction.objects.filter(slug="how-to-write-activity").update(
        content=INSTRUCTION_CONTENT
    )


def revert_instruction_content(apps, schema_editor):
    Instruction = apps.get_model("core", "Instruction")
    if not previous_content:
        return
    for pk, content in previous_content.items():
        Instruction.objects.filter(pk=pk).update(content=content)


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0013_seed_default_rates"),
    ]

    operations = [
        migrations.RunPython(update_instruction_content, revert_instruction_content),
    ]