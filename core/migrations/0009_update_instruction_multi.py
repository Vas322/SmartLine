from django.db import migrations


INSTRUCTION_CONTENT = """Запись активности на форте

Сообщение должно начинаться с + и содержать 4 части, разделённые между собой:

+<время> <разделитель> <тип> <разделитель> <ники> <разделитель> <описание>

1. Время - сколько часов: 1, 2, 0,5 (через запятую) или 0.5 (через точку).
2. Тип - деф (оплачивается) или фарм (учитывается, но не оплачивается). Регистр не важен: ДЕФ, DEF, ФАРМ - всё подойдёт.
3. Ники - один или несколько, через запятую. Только буквы и цифры (как в L2 Astrios). Один тип - одно сообщение.
4. Описание - любой текст (волна, замок, комментарий).

Разделители (любой из перечисленных, можно с пробелами вокруг или без): | (вертикальная черта), - (дефис, удобнее с русской раскладки), – или — (тире). Просто пробелы между частями НЕ являются разделителем.

Примеры (все правильные):
+1 - деф - Swettka, Pocomaxa - Первая волна
+0,5 | фарм | Ostin, Pocomaxa | вторая волна
+1 - ДЕФ - Swettka - описание
+2–фарм–Ostin–две волны

Одно сообщение = один тип (деф ИЛИ фарм). Если в волне были и деф, и фарм - пришлите два сообщения.

Если в сообщении ошибка (неизвестный формат, конфликт ника) - бот ответит прямо в группе под вашим сообщением. Исправьте сообщение редактированием - бот пересчитает его заново (повторно не засчитает).
"""


def fix_instruction_content(apps, schema_editor):
    Instruction = apps.get_model("core", "Instruction")
    Instruction.objects.filter(slug="how-to-write-activity").update(
        content=INSTRUCTION_CONTENT
    )


def revert_instruction_content(apps, schema_editor):
    # Обратная миграция намеренно не возвращает предыдущий текст.
    pass


class Migration(migrations.Migration):

    dependencies = [("core", "0008_alter_activity_telegram_message")]

    operations = [
        migrations.RunPython(fix_instruction_content, revert_instruction_content),
    ]
